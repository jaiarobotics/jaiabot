import Map from "ol/Map";
import Feature from "ol/Feature";
export declare function addPopup(map: Map, feature: Feature, popupElement: HTMLElement): void;
export declare function addPopupHTML(map: Map, feature: Feature, popupHTML: string): void;
