# Fleet config contract snapshots

One `vN.json` per `PROJECT_FLEET_CONFIG_VERSION`, generated by
`scripts/build/fleet-config-contract.py write` and checked by the build.
Never edit these by hand, and never change an older version's file: migrations
are derived from the differences between consecutive snapshots.

See `src/doc/markdown/page091_major_upgrade.md`.
