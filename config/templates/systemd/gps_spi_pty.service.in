[Unit]
Description=Script to read GPS data from SPI and send it to gpsd
Before=chrony.service
Before=gpsd.service
After=network.target

[Service]
Type=notify
User=root
ExecStart=$jaiabot_share_dir/jaiabot/python/$subdir/$exe /dev/gps0

[Install]
RequiredBy=gpsd.service
