DEBUG=true
# allocation ID for 52.36.157.57
# ELASTIC_IP_ALLOCATION_ID=eipalloc-05279e067afb49258
IPV6_ADDRESS=2600:1f13:501:a400:d6ad:3dd6:5c68:3649

ENABLE_TERMINATION_PROTECTION=false
SERVER_TYPE=vpn
REPO=release
INDENTED_ROOT_PUBKEYS=$(sed 's/^/      /' ${SCRIPT_PATH}/../../../config/ssh/root_authorized_keys)

function create_security_group_rules()
{
    local SECURITY_GROUP_ID=$1
    # Set Up Security Group Rules
    run "" aws ec2 authorize-security-group-ingress --group-id $SECURITY_GROUP_ID --ip-permissions IpProtocol=tcp,FromPort=22,ToPort=22,IpRanges='[{CidrIp=0.0.0.0/0}]',Ipv6Ranges='[{CidrIpv6=::/0}]'
    echo ">>>>>> Allowed SSH (port 22) on Security Group"

    run "" aws ec2 authorize-security-group-ingress --group-id $SECURITY_GROUP_ID --ip-permissions IpProtocol=udp,FromPort=51820,ToPort=52020,IpRanges='[{CidrIp=0.0.0.0/0}]',Ipv6Ranges='[{CidrIpv6=::/0}]'
    echo ">>>>>> Allowed Wireguard (ports 51820-52020) on Security Group"
}
